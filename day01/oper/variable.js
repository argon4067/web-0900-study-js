// 데이터타입 - 원시타입
// 숫자타입(number) : 42, 3.14, ...
// 논리타입(bool) : true, false, ... 
// 문자열 타입(string) : "안녕", '하세요', "A", 'a', ... 
// 객체 레퍼런스 타입(object) : Object(해쉬맵), Array, Math, Date, ... 
// undefined : 타입이 정해지지 않은 것을 의미한다. = 값이 없다.
// 값이 있으면 타입도 같이 넘어가니까
// null : 값이 정해지지 않은 것을 의미한다.

// 동적 바인딩 : 10만써도 number까지 같이 ram에 올려준다.

console.log(number1,number2);
var number1 = 10;
let number2 = 20;

number1= 30;
number2= 40;

const number3 =100;
number3=200;
console.log(number3);
